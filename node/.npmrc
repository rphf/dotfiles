# Global npm configuration
# Managed via dotfiles — stow target: ~/

# Temporary: point to a dead endpoint to block all installs (npm ecosystem vulnerabilities)
registry=http://127.0.0.1:9999

# Skip lifecycle scripts from third-party packages
ignore-scripts=true

# Reject packages published less than 7 days ago
min-release-age=7
